/**
 * Kurze Nutzer-erklärbare Texte (Phase 4 Stub — später mit Templates / zweiter KI-Call).
 */
export function buildResultExplanationStub(): string {
  return "Die Empfehlung basiert auf deinen Eingaben und den passenden Produkten aus dem Katalog.";
}
