/**
 * Phase 9 (Produkt-Vorfilterung gegen die DB) ist im Zielbild `src/lib/recommendation/prefilter.ts`
 * und nicht Teil der reinen Algorithmus-Berechnung hier.
 * Diese Datei hält nur die Namenskonvention aus REWRITE_PLAN / PS-2.
 */
export const PRODUCT_PREFILTER_LIVES_IN_RECOMMENDATION = true;
